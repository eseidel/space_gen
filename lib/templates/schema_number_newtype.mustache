{{{ doc_comment }}}extension type const {{{ typeName }}}._(double value) {
    const {{ typeName }}(this.value){{{validations}}};

    factory {{ typeName }}.fromJson(num json) => {{ typeName }}(json.toDouble());

    /// Convenience to create a nullable type from a nullable json object.
    /// Useful when parsing optional fields.
    static {{ nullableTypeName }} maybeFromJson(double? json) {
        if (json == null) {
            return null;
        }
        return {{ typeName }}.fromJson(json);
    }

    double toJson() => value;
}
