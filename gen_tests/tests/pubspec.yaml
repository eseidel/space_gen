name: tests
description: Tests for the generated gen_tests directory.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  types:
    path: ../types

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
